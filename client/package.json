{
  "name": "truck-dispatch-client",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "start:client": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preflight": "tsc --noEmit && vite build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}

